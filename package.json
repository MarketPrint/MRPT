{
  "name": "marketprint-tape",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "sync": "node src/cli-sync.ts",
    "reconstruct": "node src/cli-reconstruct.ts",
    "verify": "node src/cli-verify.ts",
    "check": "tsc --noEmit",
    "test": "node --test test/*.test.ts",
    "readme": "node src/cli-readme.ts"
  },
  "dependencies": {
    "viem": "^2.55.10"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "typescript": "^5.9.3"
  }
}
